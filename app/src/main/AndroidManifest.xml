<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- IZIN INTERNET -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Yanda"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".BaseActivity"
            android:exported="false" />
        <!-- WEBVIEW -->
        <activity
            android:name=".WebViewActivity"
            android:exported="false" /> <!-- SPLASH SCREEN -->
        <activity
            android:name=".SplashScreenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- LOGIN -->
        <activity
            android:name=".Home.pertemuan3.LoginActivity"
            android:exported="false" /> <!-- MAIN -->
        <activity
            android:name=".MainActivity"
            android:exported="false" /> <!-- BANGUN RUANG -->
        <activity
            android:name=".Home.pertemuan2.BangunRuangActivity"
            android:exported="false" /> <!-- CUSTOM 1 -->
        <!-- CUSTOM 2 -->
        <activity
            android:name=".Home.pertemuan4.Custom2Activity"
            android:exported="false" /> <!-- WELCOME -->
        <activity
            android:name=".Home.pertemuan3.WelcomeActivity"
            android:exported="false" />
    </application>

</manifest>