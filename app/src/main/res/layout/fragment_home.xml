<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:background="@color/background">

    <!-- TOOLBAR - TAMBAHKAN INI -->
    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="0dp"
        android:layout_height="?attr/actionBarSize"
        android:background="@color/primary"
        android:title="Bina Desa"
        android:titleTextColor="@android:color/white"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <TextView
        android:id="@+id/tvWelcome"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Selamat Datang di Bina Desa"
        android:textSize="24sp"
        android:textStyle="bold"
        android:textColor="@color/text_primary"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@id/toolbar"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- BUTTON 1 -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnBangunRuang"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Pertemuan 2: Bangun Ruang"
        android:textColor="@color/white"
        android:layout_marginTop="24dp"
        app:backgroundTint="@color/primary"
        app:cornerRadius="12dp"
        app:layout_constraintTop_toBottomOf="@id/tvWelcome"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- BUTTON 2 -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnCustom1"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Pertemuan 3: Custom 1"
        android:textColor="@color/white"
        android:layout_marginTop="12dp"
        app:backgroundTint="@color/primary_dark"
        app:cornerRadius="12dp"
        app:layout_constraintTop_toBottomOf="@id/btnBangunRuang"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- BUTTON 3 -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnCustom2"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Pertemuan 4: Custom 2"
        android:textColor="@color/white"
        android:layout_marginTop="12dp"
        app:backgroundTint="@color/accent"
        app:cornerRadius="12dp"
        app:layout_constraintTop_toBottomOf="@id/btnCustom1"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- BUTTON 4 -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnWebsite"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Website Desa"
        android:textColor="@color/white"
        android:layout_marginTop="12dp"
        app:backgroundTint="@color/teal_700"
        app:cornerRadius="12dp"
        app:layout_constraintTop_toBottomOf="@id/btnCustom2"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- LOGOUT -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnLogout"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Logout"
        android:textColor="@color/white"
        android:layout_marginTop="32dp"
        app:backgroundTint="@color/button_logout"
        app:cornerRadius="12dp"
        app:layout_constraintTop_toBottomOf="@id/btnWebsite"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>